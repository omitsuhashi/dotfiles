interface:
  display_name: "Review Fix Loop"
  short_description: "Help with Review Fix Loop tasks"
  default_prompt: "Use $review-fix-loop with $requesting-code-review and $receiving-code-review, and run /review explicitly each round until review findings are zero."
