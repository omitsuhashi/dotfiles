interface:
  display_name: "Review Fix Loop"
  short_description: "Help with Review Fix Loop tasks"
  default_prompt: "Use $review-fix-loop. Run explicit /review each round with requesting-code-review/code-reviewer.md, then handle findings with $receiving-code-review until findings are zero."
